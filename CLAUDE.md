# meshi-gacha

予算内でランダムに注文が決まるガチャ風 Web アプリ。UI/ロジックは `index.html`
（HTML/CSS/JS 一体）、店舗メニューデータは `stores.json` に分離。ビルド工程は
ないが、`stores.json` を `fetch` で読むため **http(s) 配信が前提**（GitHub Pages
等）。`file://` 直開きは CORS によりメニューが読めず非対応。

## 応答方針

- ユーザーへの返答・計画（プラン）・要約・確認質問はすべて**日本語**で行う。
  （コード・コミットメッセージ等の成果物は従来どおり文脈に応じてよい。）

## 開発メモ

- 店舗データは `stores.json`（リポジトリ直下）で定義し、起動時に `fetch` で
  読み込んで `STORES` に充填する（`index.html` 内は `let STORES={}` の器のみ）。
  各店舗に `name / short / emoji / tag / theme / spec / story / menu` を持つ。
  メニュー追加・価格改定は `stores.json` を編集する（`index.html` ではない）。
- サイゼリヤは JST 22:00〜翌5:00 の結果に深夜料金 +10% を加味し、加味後でも
  予算内に収まるよう `computeResult()` が小計上限を圧縮する。
- `theme`（`rosso / rossoDark / verde / oro`）は `applyStore()` で CSS 変数に反映され、
  筐体（ガチャマシン）の配色が店舗ごとに切り替わる。

## バージョニング方針

- セマンティックバージョニング（`MAJOR.MINOR.PATCH`）を採用する。
  - PATCH: 後方互換のバグ修正・微調整。
  - MINOR: 後方互換の機能追加・改善。
  - MAJOR: 破壊的変更（大幅な仕様変更・互換性の喪失）。
- **MAJOR バージョンの変更は必ず事前にユーザー確認を取る**（自動で上げない。
  必要と判断したら理由を添えて提案し、承認を得てから変更する）。
  MINOR / PATCH は方針に沿って都度更新してよい。
- 現行バージョンは `0.33.1`。表示は `index.html` ヘッダーの `.ver` バッジ
  （`<button class="ver" id="verBadge">v0.33.1</button>`）が唯一の表示箇所。
  バージョン更新時はここを書き換える。タイトル（`<head><title>` /
  `document.title`）は「飯ガチャ」固定でバージョンは含めない。
- バージョンバッジをクリックするとアップデート履歴モーダル
  （`#changelogModal`）が開く。新しい変更を出したら `index.html` 末尾の
  `CHANGELOG` 配列の先頭に追記する（最新が先頭）。文言は一般ユーザー
  視点でわかりやすく書くこと。

## 運用方針

- **作業開始時はまず `origin/main` を取得し、作業ブランチへ取り込んでから
  着手する**（並行セッションで `main` が頻繁に進むため。コミット／PR の
  直前にも再取得してコンフリクトを最小化する）。
- このリポジトリでは、ユーザーへの都度確認なしにブランチ／PR を `main` へ
  マージしてよい（事前承認済み）。それ以外のリスクのある操作（force push、
  履歴の破壊的変更、`main` への直接 push など）は従来どおり都度確認する。
- **プランなし（プランモードでの事前承認を経ていない）実装では、完了後の
  `main` へのマージを確認なしで実施する**（「マージしてよいか」を都度
  尋ねない。上記のマージ事前承認の明確化）。プランモードで承認を得た
  場合はそのプランの範囲に従う。
- **UI 変更でスクリーンショット／ブラウザ目視確認を必須としない**。環境に
  ブラウザが無い場合は無理に検証せず、コード整合・構文チェック等で足りる
  なら完了としてよい（「未検証」を毎回ことわる必要はない。検証が必要だと
  判断した場合や求められた場合のみ言及する）。

## モデル運用（複雑タスク時の自動エスカレーション）

- Claude Code 自体には複雑さ判定でモデル／effort を自動切替する機能はない。
  代替として、タスクが複雑・多段だと判断したら次を自発的に行う：
  - 拡張思考を深める（必要に応じ `ultrathink` 相当の深い推論で着手）。
  - 重い調査や並列可能な作業はサブエージェント（Task）へ委譲して
    メインのコンテキストを保護する。
  - さらに深い推論が有効な場面では、ユーザーに `/effort max` への
    引き上げを提案する（ハーネス側は自動変更できないため提案にとどめる）。
- 単純なタスクでは上記を発動せず、簡潔・低コストに処理する。
